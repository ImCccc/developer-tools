{
  "name": "react-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "dev-service-start"
  },
  "lint-staged": {
    "src/**/*.{ts,tsx}": [
      "eslint --cache --fix",
      "git add"
    ],
    "src/**/*.{js,jsx}": [
      "eslint --cache --fix",
      "git add"
    ]
  },
  "dependencies": {
    "lcr-tools-service": "^1.0.0"
  },
  "devDependencies": {}
}
